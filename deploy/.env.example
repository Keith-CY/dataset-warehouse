POSTGRES_USER=lakefs
POSTGRES_PASSWORD=replace-with-postgres-password
POSTGRES_DB=lakefs

MINIO_ROOT_USER=datasetwarehouse
MINIO_ROOT_PASSWORD=replace-with-minio-password
MINIO_API_PORT=9000
MINIO_CONSOLE_PORT=9001

LAKEFS_AUTH_ENCRYPT_SECRET_KEY=replace-with-long-random-secret
LAKEFS_BLOCKSTORE_BUCKET=lakefs-blockstore
LAKEFS_BLOCKSTORE_S3_REGION=us-east-1
LAKEFS_PORT=8000

# Pin these in production instead of relying on latest.
POSTGRES_IMAGE_TAG=16
MINIO_IMAGE_TAG=latest
MINIO_MC_IMAGE_TAG=latest
LAKEFS_IMAGE_TAG=latest
